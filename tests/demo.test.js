describe('Pruebas <LearnigComponent/>', () =>
{
  test('Esta prueba no debe de fallar', () =>
  {

    // 1,. Inicializar

    const lMessage1 = 'Hola mundo';

    // 2.- Estimulo

    const lMessage2 = lMessage1.trim();

    // 3,. Observar el comportamiento
    //expect (lMessage1).toBe(lMessage2);
    expect(lMessage1).toBe(lMessage2);

  })


  test('Esta prueba no debe de fallar2', () =>
  {

    // 1,. Inicializar

    const lMessage1 = 'Hola mundo';

    // 2.- Estimulo

    const lMessage2 = lMessage1.trim();

    // 3,. Observar el comportamiento
    //expect (lMessage1).toBe(lMessage2);
    expect(lMessage1).toBe(lMessage2);

  })
})


